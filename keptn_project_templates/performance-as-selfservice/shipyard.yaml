apiVersion: "spec.keptn.sh/0.2.0"
kind: "Shipyard"
metadata:
  name: "shipyard-performance-as-selfservice"
spec:
  stages:
  - name: "performance"
    sequences:
      - name: testing
        tasks:
        - name: test
          properties:
            teststrategy: performance
        - name: evaluation